name: caravela
version: '1.0.0'
config-version: 2

profile: caravela

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  caravela:
    staging:
      +materialized: view
    marts:
      +materialized: table
