# Changelog — Project Caravela
# REQ-037.2: Record all ad hoc changes and deviations during implementation.
# Format: Date | Component | Change | Reason

| 2026-03-11 | `date_key` type | Confirmed `DATE` type for `dim_date.date_key` and all FK references in `fct_sales`, `fct_payments`, `fct_reviews` | `dbt_utils.date_spine` produces DATE natively — no extra casting in `dim_date`. Staging models cast source timestamps via `DATE(CAST(... AS TIMESTAMP))`. Same-type FK joins across all fact tables. INTEGER (YYYYMMDD) would require additional casting in both `dim_date` and staging with no BigQuery benefit. REQ-008.1 Open Item (3) and REQ-017.1 blocking flag closed. |
| 2026-03-11 | BigQuery datasets | Renamed `raw` → `olist_raw`; `analytics` → `olist_analytics` | `raw` is a reserved word in BigQuery standard SQL. Using it as a dataset name requires backtick-quoting in every generated query; the dbt BigQuery adapter handles this in some versions but not all — behaviour is version-dependent and not documented as a guarantee. Renaming eliminates the ambiguity at zero cost pre-implementation. Affects: `meltano.yml` target dataset config, `dbt_project.yml`, `sources.yml`, Dagster `DbtProject` config, `scripts/generate_parquet.py` (default `--dataset` argument), notebooks. BRD updated to v2.6; REQ-003.1 Open Item 2 closed. |
| 2026-03-14 | Meltano tap plugin | Switched from `tap-spreadsheets-anywhere` to `tap-csv` (MeltanoLabs variant) | `tap-csv` handles all 9 CSVs correctly including the UTF-8 BOM on `product_category_name_translation.csv` (no `encoding: utf-8-sig` needed — Python csv module strips BOM transparently). 1M-row geolocation file loaded in ~31s. Simpler config structure (`entity`/`path`/`keys` vs `tables` array). |
| 2026-03-14 | Meltano target method | Changed `target-bigquery` method from `storage_write_api` to `batch_job` | `storage_write_api` holds open gRPC streams per table. When `tap-csv` processes files sequentially, earlier streams go idle during the 31s geolocation load and hit BigQuery's 600s inactivity timeout — `olist_order_items_dataset` stream was killed. `batch_job` uses traditional load jobs with no persistent streams, immune to idle timeouts. Trade-off: slower (~34 min total) but reliable for sequential taps. |
| 2026-03-14 | BigQuery table naming — `_view` suffix | `target-bigquery` with `denormalized: false` stores data as JSON blobs in base tables and creates flat-column views with `_view` suffix. dbt `sources.yml` must reference the view names (e.g. `olist_customers_dataset_view`) instead of the original stream names. | **DOWNSTREAM IMPACT (Agent 1b):** `sources.yml` table names must ALL have `_view` suffix appended. Full mapping: `olist_customers_dataset_view`, `olist_orders_dataset_view`, `olist_order_items_dataset_view`, `olist_order_payments_dataset_view`, `olist_order_reviews_dataset_view`, `olist_products_dataset_view`, `olist_sellers_dataset_view`, `olist_geolocation_dataset_view`, `product_category_name_translation_view`. Base tables contain a `data` JSON column — do NOT query base tables directly. Also applies to Dagster `AssetKey` values (Agent 1c) and `AssetSpec` names. |
| 2026-03-14 | dbt staging — `sources.yml` adapted to `_view` suffix | All 9 `sources.yml` table names use `_view` suffix to match `target-bigquery` `denormalized: false` output. Spec originally listed names without suffix. | Upstream deviation from Agent 1a (changelog 2026-03-14 `_view` suffix entry). `sources.yml` is the naming contract — all staging models use `source('olist_raw', '..._view')`. No functional impact on downstream marts (they reference `ref('stg_*')`, not raw sources). |
| 2026-03-14 | dbt staging — `stg_products` uses `product_category_name_english` column | Spec referenced `string_field_1` for the English translation column. Actual column name from `tap-csv` is `product_category_name_english` (CSV header preserved). | `tap-csv` preserves CSV headers unlike raw BigQuery uploads which auto-name columns. Confirmed via `docs/data_profile.json`. |
| 2026-03-14 | dbt — protobuf exit crash on `dbt deps`/`dbt parse`/`dbt compile` | `dbt` exits with code 1 due to `TypeError: MessageToJson() got an unexpected keyword argument 'always_print_fields_with_no_presence'`. Parse and compile complete successfully before the crash — `manifest.json` is generated correctly. | Known `protobuf` / `dbt-common` version incompatibility in dbt 1.11.7. The crash occurs in post-execution event logging, not in parsing/compilation. All 9 models and 9 sources register correctly. Workaround: check `manifest.json` existence rather than exit code. |
| 2026-03-15 | dbt testing — `fct_payments.date_key` FK test omitted | Spec listed `fct_payments.date_key → dim_date` as a relationships test target. Omitted in `marts/schema.yml`. | `fct_payments` uses `LEFT JOIN stg_orders` — payments for orders missing from `stg_orders` will have NULL `date_key`. A `relationships` test on a nullable FK column fails for every NULL row. Adding `not_null` would also fail legitimately. The compound PK (`order_id`, `payment_sequential`) uniqueness test is retained; the `assert_date_key_range.sql` singular test guards out-of-range dates on non-null values. |
| 2026-03-15 | `stg_products` — COALESCE fixed for 610 empty-string product_category_name rows | Replaced `CAST(p.product_category_name AS STRING)` (second COALESCE arg) with `CASE WHEN TRIM(IFNULL(..., '')) = '' THEN NULL ELSE ... END`. tap-csv encodes missing CSV cells as `''` not NULL — the original COALESCE stopped at the empty string, never reaching `'uncategorized'`. Fix converts empty/blank strings to NULL so the fallback is reachable. 610 products now correctly labelled `'uncategorized'`. 13 products with untranslated Portuguese names (`portateis_cozinha_e_preparadores_de_alimentos`, `pc_gamer`) retain their Portuguese name — that branch is unchanged. **Downstream note:** `product_category_name` (raw Portuguese) retains the empty string for those 610 products. All analysis, dashboard filters, and Parquet exports must use `product_category_name_english` — not `product_category_name`. Documented in CLAUDE.md Analysis Scope section and `dim_products` schema.yml column description. |
| 2026-03-15 | dbt testing — `stg_reviews` comment fill-rate tests changed to length check | `expect_column_values_to_not_be_null(mostly=0.08/0.40)` replaced with `expect_column_value_lengths_to_be_between(min_value=1, mostly=0.08/0.40)` | `tap-csv` loads empty CSV cells as empty strings `""` not NULLs. Diagnostic query confirmed 0 NULLs in `review_comment_title`/`review_comment_message` — null-based test would trivially pass at 100% and provide no quality guard. Length-based test correctly catches blank strings. Observed fill rates: title=11.7%, message=41.3% — thresholds 0.08/0.40 retained. |
| 2026-03-15 | dbt testing — 10 staging models in `schema.yml` vs 9 in spec | `schema.yml` covers 10 staging models including `stg_product_category_name_translation` (created by Agent 1b). Spec listed 9 staging models. | Extra staging model correctly included with `not_null` test on `product_category_name`. No functional impact on downstream tests. |
| 2026-03-15 | `stg_orders` — all nullable timestamp CAST → SAFE_CAST | Changed `CAST(order_approved_at AS TIMESTAMP)`, `CAST(order_delivered_carrier_date AS TIMESTAMP)`, `CAST(order_delivered_customer_date AS TIMESTAMP)`, `CAST(order_estimated_delivery_date AS TIMESTAMP)` to `SAFE_CAST`. | `tap-csv` encodes missing CSV cells as `''` not NULL. `CAST('' AS TIMESTAMP)` raises a BigQuery error when queried; `SAFE_CAST` returns NULL silently. Only nullable timestamps affected (order_purchase_timestamp is mandatory). |
| 2026-03-15 | `stg_products` — nullable numeric columns CAST → SAFE_CAST | Changed all `CAST(p.product_name_lenght AS INT64)`, `CAST(p.product_description_lenght AS INT64)` etc. to `SAFE_CAST`. | Same `tap-csv` empty-string encoding issue. Rows where these fields are blank in the source CSV would raise errors on `CAST`. `SAFE_CAST` returns NULL; `not_null` tests removed from `product_name_length`/`product_description_length` in both `staging/schema.yml` and `marts/schema.yml`. |
| 2026-03-15 | `stg_orders` temporal pair tests — two removed due to genuine source data violations | Removed `expect_column_pair_values_A_to_be_greater_than_B` for `order_delivered_carrier_date >= order_approved_at` (1359 violations) and `order_delivered_customer_date >= order_delivered_carrier_date` (23 violations). | Timestamp ordering violations exist in the Olist source data — logistics anomalies where carrier/delivery timestamps were recorded out of order. Retained only `order_approved_at >= order_purchase_timestamp` (0 violations). Violations documented here rather than masked with `mostly` thresholds. |
| 2026-03-15 | `stg_products` — WHERE filter removed; was incorrectly excluding 610 products | Removed `WHERE TRIM(IFNULL(p.product_category_name, '')) != ''` from `stg_products.sql`. | The WHERE clause was added to address the COALESCE dead-branch issue, but CASE/NULLIF inside COALESCE already handles empty-string categories correctly. The WHERE filter excluded 610 products from `dim_products`, causing 1603 FK violations in `fct_sales.product_id → dim_products` (those product IDs still appear in order items). Fix: remove WHERE; COALESCE assigns `'uncategorized'` to empty-category products. |
| 2026-03-15 | `assert_payment_reconciliation.sql` — threshold raised from >$1.00 to >$20.00; single-installment filter added | Added `AND MAX(fp.payment_installments) = 1` to HAVING clause; changed threshold from `> 1.00` to `> 20.00`. | Diagnostic query (2026-03-15) confirmed 249 violations at $1.00 threshold. Root causes: (1) 236 multi-installment orders: Olist `payment_value` includes credit card interest (parcelamento com juros), which inflates payment_total above price+freight_value — this is expected, not a defect. (2) 13 single-installment orders: genuine Olist freight-subsidy anomalies with diffs up to R$16.50 (avg R$6.15). Threshold of R$20.00 covers all known anomalies while still catching model bugs (wrong JOIN or double-counting would produce 10x+ errors). After fix: 0 violations. |
| 2026-03-16 | `notebooks/utils.py` — `add_region()` signature updated | Added default `state_col="customer_state"`, `.copy()` for safety, kept dynamic output naming (`customer_state` → `customer_region`, `seller_state` → `seller_region`). | Spec defined output as always `"region"`, but Parquet schemas require distinct column names (`customer_region`, `seller_region`). Dynamic naming aligns with downstream contracts. `.copy()` prevents in-place mutation bugs. |
| 2026-03-16 | `notebooks/utils.py` — colour values kept as Flat UI (not Material Design) | REGION_COLOURS and SEGMENT_COLOURS retain existing Flat UI hex values instead of the Material Design values in the agent directive. | Stub was placed by orchestrator with internally consistent Flat UI palette. Changing to Material Design would be cosmetic only. Both palettes provide sufficient contrast for adjacent choropleth regions. |
| 2026-03-16 | REQ-020.2 — BigQuery connector uses `google.cloud.bigquery` instead of SQLAlchemy | All 4 notebooks use `google.cloud.bigquery.Client` directly instead of SQLAlchemy + `sqlalchemy-bigquery`. | Simpler API, fewer dependencies, native BigQuery types. `client.query(sql).to_dataframe()` is more direct than SQLAlchemy's `create_engine` + `pd.read_sql`. No functional difference in read-only queries. |
| 2026-03-16 | `03_geo_seller_analysis.ipynb` — seller cancellation rate bug fix | Changed `COUNTIF(so.order_status = 'canceled')` → `COUNT(DISTINCT CASE WHEN so.order_status = 'canceled' THEN so.order_id END)` in seller performance query numerator. | Original used item-level counting in numerator (COUNTIF counts rows) but order-level in denominator (COUNT DISTINCT). A canceled order with 4 items produced rate=4.0 (400%). 9 sellers affected (all ≤4 orders). Post-fix max=1.0, mean dropped from 0.0294 to 0.0234. Parquet re-exported. |
| 2026-03-16 | Quality audit — narrative corrections across NB01, NB02, NB03 | Fixed 5 inaccurate claims in Key Findings markdown cells: (1) NB03: "USD 3.2M" → "USD 4.3M"; (2) NB01: "CC AOV R$166 vs boleto R$145" → "R$152 vs R$127"; (3) NB03: "SP drives 40%+" → "~37%"; (4) NB02: "sharpest drop is on-time→1-3d late" → "1-3d→4-7d late (1.19 pts)"; (5) NB01: "100% GMV growth" → "~103%". | All claims validated against Parquet data and BQ query outputs. No code changes — markdown narrative only. Corrected to prevent misleading presentation findings. |
| 2026-03-16 | `notebooks/utils.py` — added Lorenz/Gini/HHI helper functions | Added `import numpy as np` and 4 new functions: `lorenz_curve()`, `gini_coefficient()`, `hhi()`, `concentration_summary()`. | Spec says "Pure Python only" — interpreted as no-Streamlit constraint (original intent). numpy is a transitive dependency of pandas; all consumers already have it. Functions are used by NB01, NB02, NB03 and available to Agent 4's dashboard via `from notebooks.utils import lorenz_curve, gini_coefficient`. |
| 2026-03-16 | Lorenz/Gini concentration analysis added to NB01, NB02, NB03 | NB01: category revenue Lorenz + Gini (0.71). NB02: customer spending Lorenz + Gini (0.48) + regional breakdown. NB03: seller GMV Lorenz + Gini (0.78) + temporal Gini trend (Jan 2017→Aug 2018: 0.63→0.66, slowly increasing) + category-level seller HHI (7 of 60 categories concentrated). | Extends the existing Pareto analysis with standardised economics metrics (Gini, HHI, CR4/CR10). Two new BQ queries in NB03 (monthly seller GMV, category×seller GMV). No changes to existing 5 Parquet schemas. |
| 2026-03-16 | New Parquet: `data/concentration_metrics.parquet` (83 rows) | Pre-computed Gini, CR4, CR10, HHI, top_20pct_share across 5 dimensions: seller_gmv (overall), seller_gmv_monthly (20 months), category_seller_gmv (60 categories), customer_monetary (overall), category_revenue (overall). | New downstream contract for Agent 4. Exported from NB03 (self-contained — re-reads NB01/NB02 Parquets to avoid cross-notebook dependencies). Dashboard can display Gini as KPI cards and temporal trend as line chart without recomputation. |
| 2026-03-16 | Quality audit — concentration analysis interpretations fixed | 5 fixes: (1) NB03 category chart sorted by HHI instead of Gini, title changed to "Category Monopoly Risk"; (2) NB03 Key Finding #7 now includes `small_appliances_home_oven_and_coffee` (HHI 3734) as #2 concentrated category; (3) NB03 seller Gini interpretation reframed — CR4=6.1% is positive (no monopoly), not alarming; (4) NB01 category interpretation distinguishes Gini (inequality) from HHI (no dominance); (5) NB02 removed forward-reference to seller Gini, replaced with standalone moderate assessment. | Gini measures inequality (long-tail distribution), HHI measures monopoly risk (market power). High Gini + low HHI = healthy inequality pattern. Original code/narratives conflated the two, leading to misleading conclusions (e.g., `watches_gifts` Gini 0.85 ranked as "most concentrated" despite competitive HHI 919). All 3 notebooks re-executed and Parquets re-exported. |
| 2026-03-16 | All notebooks + `generate_parquet.py` — `load_dotenv()` integration | Added `python-dotenv` to all 4 notebooks and `scripts/generate_parquet.py`. BigQuery dataset names now read from `.env` variables (`BIGQUERY_ANALYTICS_DATASET`, `BIGQUERY_RAW_DATASET`) instead of being hardcoded. Fallback defaults (`olist_analytics`, `olist_raw`) preserve backwards compatibility. NB00 uses both `dataset` and `raw_dataset` variables (only notebook querying `olist_raw`). NB01–NB03 use `dataset` only. `generate_parquet.py` `--dataset` CLI arg now defaults to `$BIGQUERY_ANALYTICS_DATASET` from env. | Centralises configuration in `.env` — no more need to `export` env vars in shell before running notebooks. **Downstream note (Agent 4):** No impact on dashboard — it reads Parquet files, not BigQuery. If Agent 4 ever adds `load_dotenv()` to `dashboard_utils.py`, it should use `load_dotenv(Path(__file__).parent / ".env")` (same project root). `python-dotenv` is already installed in the `assignment2` conda env. |
| 2026-03-16 | `docs/data_dictionary.md` — Data Analyst draft (REQ-063.1) | Created analyst portion: all 6 Parquet file schemas with column types and derivation logic, 11 metric definitions, `utils.py` API reference, data quality caveats. | Data Engineer to add raw source columns, staging transformation details, and dbt column descriptions. |
| 2026-03-16 | `docs/dashboard_user_guide_analyst_draft.md` — Data Analyst draft (REQ-050.1) | Created analyst portion: per-page interpretation guidance, RFM segment action table, filter applicability matrix, delay×satisfaction threshold insight, 6 data quality notes for honest interpretation. | Dash Engineer to merge with technical operation sections (filter behaviour, layout, navigation, deployment). Blocked on REQ-024.1 (dashboard not yet built). |
| 2026-03-16 | `scripts/generate_parquet.py` — complete rewrite to match notebook schemas | Switched from SQLAlchemy to `google.cloud.bigquery.Client` (matching notebooks). Fixed 10+ column naming mismatches: `dp.product_category`→`product_category_name_english`, `R_score`→`r_score`, `monetary`→`monetary_value`, `F_tier` uses "F1"/"F2"/"F3" strings, `delay_days`→`delivery_delay_days`, `nps_bucket`→`nps_category`, `order_count`→`total_orders`, `on_time_count`→`on_time_orders`. sales_orders reduced to exact 15-column spec (removed customer_unique_id, seller_id, seller_state, day, customer_city; added date_key). seller_performance reduced to 7-column spec (removed seller_city, geolocation_lat/lng, on_time_rate). Cancellation rate fixed to order-level COUNT DISTINCT matching NB03 fix. RFM query includes `WHERE s.date_key <= '{RFM_REF_DATE}'` matching NB02 date leak fix. Added `gen_concentration_metrics()` for 6th Parquet. Uses `GCP_PROJECT_ID` env var by default with `--project` override. | Agent 4 (Dashboard Engineer) gets identical Parquet files whether produced by notebooks or this script. All 6 downstream schemas verified against notebook outputs. |
| 2026-03-16 | Agent 4 — Dashboard Engineer: `dashboard.py`, `dashboard_utils.py`, `pages/1_Executive.py`, `pages/2_Products.py`, `pages/3_Geographic.py`, `pages/4_Customers.py`, `pages/5_Glossary.py` created. Deviations from spec: (1) Added `pages/5_Glossary.py` (not in original REQ-024.1 scope — user-requested); (2) All 4 analysis pages use `st.tabs()` horizontal tab layout (one focused story per tab); (3) Extra charts added beyond spec: Lorenz curve (Products), freight-to-price ratio (Products), delivery schedule bar (Geographic), seller quality tiers treemap (Geographic), seller Gini trend line (Geographic); (4) `geo_delivery` filter uses `region` column (not `customer_region`) via dedicated `apply_geo_filters()`; (5) KPI row pinned above tabs on Executive page for always-visible headline metrics; (6) `apply_filters()` always applies date filter using session_state date_start/date_end (initialised to full period range); (7) Segment Playbook tab added to Customer Analysis (actionable cards per RFM segment). | See agent directive for full spec.
| 2026-03-16 | Final audit — 7 fixes across all notebooks | (1) **NB02 RFM date leak fixed**: Added `WHERE s.date_key <= '{RFM_REFERENCE_DATE}'` — orders after 2018-08-31 produced negative recency_days (min was -3, now min=2); (2) **NB03 heatmap weighted mean**: Region×month delay heatmap now uses order-weighted average instead of unweighted state-average; (3) **NB01 growth text**: "~100%" → "~103%" (consistency with Key Findings); (4) **NB00/NB01 data cut table**: Clarified 2018-09 shows 1 order in `fct_sales` vs 16 in raw (15 orders had no items); (5) **Exchange rate standardised**: All notebooks now use R$3.65/USD (2018 average) → USD 4.3M; (6) **NB01/NB02/NB03 headers enriched**: Added methodology notes (item-granularity trap, payment_sequential approximation, total_sale_amount definition, 610 uncategorized products, cancellation rate bug history); (7) **NB03 Key Finding #5**: Now cross-references customer Gini 0.48 from NB02 for supply-side vs demand-side contrast. | Audit caught: RFM data leak (MEDIUM — ~20 orders leaked past reference date), unweighted heatmap averages (LOW — small states got equal weight to SP), and 5 narrative inconsistencies/gaps. All notebooks re-executed and verified. || 2026-03-16 | Dagster — `RAW_TABLES` AssetKeys use `_view` suffix | All 9 `AssetKey` values in `dagster_project/assets.py` use `_view`-suffixed names (e.g. `olist_customers_dataset_view`) instead of the spec's plain names. | Upstream deviation from Agent 1a/1b (changelog 2026-03-14 `_view` suffix entry explicitly flags Dagster AssetKey as affected). `sources.yml` and `manifest.json` both reference `_view` names — mismatched AssetKeys would silently break the Meltano→dbt dependency edge in the asset graph. |
| 2026-03-16 | Dagster — `meltano_ingest` calls `tap-csv`, not `tap-spreadsheets-anywhere` | Subprocess command is `meltano run tap-csv target-bigquery`. | Upstream deviation from Agent 1a (changelog 2026-03-14 tap plugin switch). `tap-spreadsheets-anywhere` is not installed in the Meltano project — calling it would fail at runtime. |
| 2026-03-16 | Dagster — `meltano_ingest` changed from `@asset(deps=...)` to `@multi_asset(specs=...)` | `@asset(deps=RAW_TABLES)` was creating `meltano_ingest` as DOWNSTREAM of `olist_raw/*` (raw tables were external; both `meltano_ingest` and `stg_*` ran at same topological level with no ordering guarantee). Replaced with `@multi_asset(specs=RAW_TABLE_SPECS)` which declares `meltano_ingest` as the PRODUCER of all 9 `olist_raw/*` assets. | Confirmed via `graph.executable_asset_keys` and `graph.toposorted_asset_keys`: `olist_raw/*` is now executable (not external), and topological order enforces meltano_ingest first. No conflict with `@dbt_assets` — dbt treats sources as upstream deps, not separately owned assets. |
| 2026-03-16 | `scripts/launch_dagster.sh` created | New script at `scripts/launch_dagster.sh` launches Dagster from repo root. Runs 7 pre-flight checks (dagster binary, dbt binary, `GOOGLE_APPLICATION_CREDENTIALS`, `GCP_PROJECT_ID`, `pyproject.toml`, `__init__.py`, `manifest.json` with age warning). Supports `--parse` to regenerate manifest, `--host`/`--port` overrides. Handles dbt 1.11.7 protobuf exit-code bug by checking manifest existence rather than process exit code. | Not a BRD deviation — supplementary operational tooling. |
| 2026-03-16 | Dagster — schedule uses `job_name` string instead of job object reference | `ScheduleDefinition(job_name="full_pipeline_job", ...)` used in `schedules.py` instead of passing the job object directly. | Spec pattern (`from . import full_pipeline_job` in `schedules.py`) creates a circular import: `__init__.py` imports `full_pipeline_schedule` from `schedules.py`, while `schedules.py` would import `full_pipeline_job` from `__init__.py`. Dagster resolves `job_name` string references from the `Definitions` object at load time — functionally identical, no circular import. |
| 2026-03-16 | Dagster — project-local `DAGSTER_HOME` and `.env` loading added | Created `dagster/dagster_home/dagster.yaml` with `EnvFileLoader` pointing to `../.env` (repo root). Updated `scripts/launch_dagster.sh` to source `.env` before Dagster starts and export `DAGSTER_HOME="<repo_root>/dagster/dagster_home"`. Added `dagster/dagster_home/storage/`, `logs/`, `history/`, `schedules/` to `.gitignore`; `dagster.yaml` remains tracked. | `DAGSTER_HOME` is a chicken-and-egg: Dagster cannot load `.env` to discover its home directory — `DAGSTER_HOME` must be set before Dagster starts. `launch_dagster.sh` is the authoritative launch path and resolves this. Default `~/.dagster/` would mix run history across projects. |
| 2026-03-17 | Meltano — `dataset` config switched to env var | Changed `target-bigquery` config from hardcoded `dataset: olist_raw` to `dataset: $BIGQUERY_RAW_DATASET`. Env var defined in repo root `.env` (`BIGQUERY_RAW_DATASET=olist_raw`). | Centralises dataset naming in `.env` alongside `GCP_PROJECT_ID` and `GOOGLE_APPLICATION_CREDENTIALS`. All three `target-bigquery` config keys now use env var interpolation — no hardcoded values remain. Validated via `meltano --env-file ../.env config print target-bigquery`. |
| 2026-03-17 | Meltano — `launch_meltano.sh` wrapper script created | New script at `meltano/launch_meltano.sh`. Uses `meltano --env-file ../.env` to load repo root `.env` without requiring a symlink or pre-sourcing. Two modes: `test` (validates tap-csv and target-bigquery config resolution) and `run` (executes `tap-csv → target-bigquery` pipeline). | Meltano auto-loads `.env` only from its own project directory (`meltano/`), but the repo's `.env` lives one level up. `--env-file ../.env` bridges this without symlinks. Script uses `cd "$(dirname "$0")"` so it works when called from any directory. **Downstream note (Agent 2):** Dagster's `assets.py` subprocess call may optionally add `--env-file ../. env` to the Meltano command for standalone safety, though `launch_dagster.sh` already sources `.env` into the process environment. |
| 2026-03-18 | Dagster — `meltano_ingest` subprocess adds `--env-file ../. env` | Added `"--env-file", "../.env"` to the meltano subprocess command in `dagster_project/assets.py`. `cwd` is `meltano/`, so `../` resolves to repo root `.env`. | Without this flag, `meltano_ingest` relied entirely on inheriting `BIGQUERY_RAW_DATASET` from the Dagster process environment — which is only guaranteed when `launch_dagster.sh` is used. Bare `dagster dev` would leave `$BIGQUERY_RAW_DATASET` unset, causing meltano to write to an empty-string dataset name. `--env-file` makes the asset self-sufficient regardless of launch method. |
| 2026-03-18 | `dbt/profiles.yml` — `dataset` switched to `env_var('BIGQUERY_ANALYTICS_DATASET')` | Changed `dataset: olist_analytics` to `dataset: "{{ env_var('BIGQUERY_ANALYTICS_DATASET') }}"`. | Aligns with `meltano.yml` which already uses `$BIGQUERY_RAW_DATASET` (changelog 2026-03-17). All dataset names now centralised in `.env`. No change to dbt model SQL. |
| 2026-03-18 | dbt-expectations — installed package is metaplane fork v0.6.0, not calogica v0.10.4 | `packages.yml` references `git: https://github.com/metaplane/dbt-expectations` at `revision: main`. Installed version self-reports as `0.6.0` (`dbt_packages/dbt_expectations/dbt_project.yml`). The original `calogica/dbt-expectations` was deprecated after v0.10.4 and is incompatible with dbt-core ≥1.8. Metaplane rewrote the package from scratch as its continuation. | The metaplane fork does **not** support a `mostly` parameter on any macro. Confirmed by reading macro signatures: `expect_column_values_to_not_be_null(model, column_name, row_condition=None)` — no `mostly` argument; `expect_column_value_lengths_to_be_between` — likewise. Both macros delegate to `expression_is_true` which has no proportion-threshold logic. **Downstream impact on `schema.yml` tests:** Fill-rate proportion tests originally specified for `stg_reviews.review_comment_title` (mostly=0.08), `stg_reviews.review_comment_message` (mostly=0.40), `dim_customers.geolocation_lat/lng` (mostly=0.97) cannot be implemented with this package. These tests are omitted; known fill rates are documented in column descriptions instead. No workaround exists short of switching packages (original calogica incompatible with dbt 1.11) or writing custom singular SQL proportion tests. |
