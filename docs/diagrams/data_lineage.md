# Data Lineage — Project Caravela

```mermaid
graph LR
    subgraph Raw["olist_raw (BigQuery) — 9 source tables"]
        r_cust[olist_customers_dataset_view]
        r_ord[olist_orders_dataset_view]
        r_items[olist_order_items_dataset_view]
        r_pay[olist_order_payments_dataset_view]
        r_rev[olist_order_reviews_dataset_view]
        r_prod[olist_products_dataset_view]
        r_sell[olist_sellers_dataset_view]
        r_geo[olist_geolocation_dataset_view<br/>~1M rows]
        r_cat[product_category_name_translation_view]
    end

    subgraph Staging["dbt Staging — All STRING → typed casts"]
        stg_cust[stg_customers]
        stg_ord[stg_orders]
        stg_items[stg_order_items]
        stg_pay[stg_payments<br/><i>filters 3 not_defined rows<br/>clamps 0→1 installments</i>]
        stg_rev[stg_reviews<br/><i>ROW_NUMBER dedup on review_id<br/>789 duplicates removed</i>]
        stg_prod[stg_products<br/><i>COALESCE: english → portuguese<br/>→ uncategorized</i>]
        stg_sell[stg_sellers]
        stg_geo[stg_geolocation<br/><i>Brazil bounding-box filter<br/>AVG lat/lng per zip<br/>~1M → ~8.5k rows</i>]
        stg_cat[stg_product_category<br/>_name_translation]
    end

    subgraph Marts["dbt Marts — olist_analytics (Star Schema)"]
        subgraph Dims["Dimensions"]
            dim_cust["🔵 dim_customers<br/>PK: customer_unique_id<br/>city · state · zip · lat · lng"]
            dim_prod["🔵 dim_products<br/>PK: product_id<br/>product_category_name_english"]
            dim_sell["🔵 dim_sellers<br/>PK: seller_id<br/>city · state · zip · lat · lng"]
            dim_date["🔵 dim_date<br/>PK: date_key (DATE)<br/>Generated: dbt_utils.date_spine<br/>2016-01-01 → 2018-12-31"]
        end

        subgraph Facts["Facts"]
            fct_sales["🟠 fct_sales<br/>Grain: order-item<br/>~112k rows<br/>total_sale_amount = price + freight"]
            fct_rev["🟠 fct_reviews<br/>PK: review_id<br/>~97k rows after dedup"]
            fct_pay["🟠 fct_payments<br/>PK: order_id + payment_sequential"]
        end
    end

    generator["⚙️ dbt_utils.date_spine<br/>(generated — no raw source)"]

    %% Raw → Staging
    r_cust --> stg_cust
    r_ord --> stg_ord
    r_items --> stg_items
    r_pay --> stg_pay
    r_rev --> stg_rev
    r_prod --> stg_prod
    r_sell --> stg_sell
    r_geo --> stg_geo
    r_cat --> stg_cat

    %% Dual-source join in staging
    stg_cat -.->|"JOIN on\nproduct_category_name"| stg_prod

    %% Staging → Dims
    stg_cust --> dim_cust
    stg_geo -->|"lat/lng by zip"| dim_cust
    stg_prod --> dim_prod
    stg_sell --> dim_sell
    stg_geo -->|"lat/lng by zip"| dim_sell

    %% dim_date generated
    generator --> dim_date

    %% fct_sales: 3-source CTE
    stg_items -->|"order_id"| fct_sales
    stg_ord -->|"order_id → customer_id"| fct_sales
    stg_cust -->|"customer_id →\ncustomer_unique_id\n(3-source resolution)"| fct_sales

    %% fct_reviews: FK to stg_orders (NOT fct_sales)
    stg_rev --> fct_rev
    stg_ord -.->|"FK: order_id → stg_orders\n(NOT fct_sales — 756\nitemless orders)"| fct_rev

    %% fct_payments: date_key from stg_orders
    stg_pay --> fct_pay
    stg_ord -.->|"date_key via\norder_purchase_timestamp"| fct_pay

    %% Fact → Dim FKs
    fct_sales -.->|"FK"| dim_cust
    fct_sales -.->|"FK"| dim_prod
    fct_sales -.->|"FK"| dim_sell
    fct_sales -.->|"FK"| dim_date
```

## Key Lineage Notes

1. **`fct_sales` is a 3-source model**: `stg_order_items.order_id` → `stg_orders.customer_id` → `stg_customers.customer_unique_id`. Direct join of `stg_order_items` to `stg_customers` produces zero matches.

2. **`fct_reviews.order_id` → `stg_orders`**, not `fct_sales`: 756 orders have reviews but no items (no `fct_sales` rows). See ADR-003.

3. **`fct_payments` depends on `stg_orders`** for `date_key` (derived from `order_purchase_timestamp`), not from `stg_payments`.

4. **`stg_geolocation`** reduces ~1M raw rows to ~8.5k zip-level aggregates via Brazil bounding-box filter + AVG(lat, lng).

5. **`stg_reviews`** deduplicates on `review_id` (789 duplicates) using `ROW_NUMBER() OVER (PARTITION BY review_id ORDER BY review_answer_timestamp DESC)`.

6. **`dim_date`** is generated by `dbt_utils.date_spine` — it has no raw data source.

7. **`stg_products`** is the only dual-source staging model, joining `olist_products_dataset_view` with `product_category_name_translation_view`.
